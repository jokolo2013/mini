import 'package:flutter/material.dart';

const pColor = Color(0xFF1B5E20);

const sColor = Color(0xFF1B5E20);

const tColor = Color(0xFF1B5E20);

const boxSize = 20.0;

const appBarC = Color(0xFF1B5E20);
const menuC = Color(0xFF1B5E20);
const backgroundC = Color(0xFF1B5E20);
const LabelWhC = Colors.white;

class sendOneKey {
  final String key;
  const sendOneKey(this.key);
}

class sendTwoKey3Property {
  final String sKey,name, details, path;
  const sendTwoKey3Property(
    this.sKey,
    this.name,
    this.details,
    this.path,
  );
}
